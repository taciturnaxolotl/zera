{
  "name": "zera",
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "serve": "bun run scripts/dev.ts",
    "build": "rm -rf .zola-build && bun run scripts/build.ts",
    "preprocess": "bun run scripts/preprocess.ts",
    "gen-og": "bun run scripts/genOG.ts"
  },
  "dependencies": {
    "@types/cli-progress": "^3.11.6",
    "cli-progress": "^3.12.0",
    "dotenv": "^16.4.7",
    "glob": "^13.0.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "puppeteer": "^23.6.0"
  }
}

